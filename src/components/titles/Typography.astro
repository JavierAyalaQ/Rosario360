---
// src/components/Typography.astro
interface Props {
    as?: string;
    classList?: string;
    body: string;
}

const { as: Tag = 'p', classList = '', body } = Astro.props;
---

<Tag class={classList} style={{ fontFamily: 'Patrick Hand' }}>
    {body}
</Tag>